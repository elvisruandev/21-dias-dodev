//Criando a variável
let valor = Number(prompt("Escolha um número inteiro e positivo"))

//Criando loop for (contagem de 0 até o número da variável "valor")
for(let contador = 0; contador <= valor; contador++){
    console.log(contador)
}